<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IPQuest"
        tools:targetApi="31">
        <activity
            android:name=".UpcomingDeadlines"
            android:exported="true" />
        <activity
            android:name=".ManageIP"
            android:exported="true" />
        <activity
            android:name=".IPList"
            android:exported="true" />

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".AddIPActivity"
            android:exported="true" /> <!-- Main activity, exported to be launched -->
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- Sign-up activity should be accessible, exported -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true" /> <!-- Login activity should be accessible, exported -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" /> <!-- Splash screen activity -->
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>